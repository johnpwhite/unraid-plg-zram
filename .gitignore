# Dev-only dependencies — dev tooling only, never ships to users
node_modules/
vendor/
composer.lock
package-lock.json

# Test artefacts
.phpunit.result.cache
coverage/
.vitest/

# Signed release-gate artifact (local, HMAC-signed, machine-specific — never committed)
.ci-artifacts/

# Build debris — generated by publish/install scripts, never committed
*.plg.bak
*.xml.bak
src.tar.gz

# IDE
.vscode/
.idea/
.DS_Store
